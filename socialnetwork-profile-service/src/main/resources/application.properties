# Application name
spring.application.name=profile-service

# Application port
server.port=8081

# Profile
spring.profiles.active=local

# Spring Cloud: Config
spring.config.import=optional:configserver:http://socialnetwork-config-server:8888